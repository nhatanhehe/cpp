for(int k=0;k<n;k++){
        for(int mask=1;mask<(1<<n);mask++){
            if((mask>>k)&1) dp[mask]=add(dp[mask],dp[Off(mask,k)]);
        }
    }
for(int k=0;k<m;k++){
        for(int mask=0;mask<(1<<m);mask++){
            if((mask>>k)&1) f[mask]=sub(f[mask],f[Off(mask,k)]);
        }
    }
